# Task ID: 8
# Title: Develop ArXiv API Integration Utilities and Configuration
# Status: pending
# Dependencies: None
# Priority: high
# Description: Create utility functions for interacting with the ArXiv API (search, download papers) and manage related configurations like PDF download directory.
# Details:
Create a new module (e.g., `arxiv_utils.py`). Implement functions using the `arxiv` Python library: 1. `search_papers(query, max_results, sort_by, date_range_filter)`: to search ArXiv. 2. `download_paper(paper_result_object_or_id, download_dir)`: to download a paper's PDF. Handle API keys (if needed by library version or direct calls), rate limiting considerations, and API errors gracefully. Add configuration for `pdfs_dir` (e.g., `arxiv_pdfs_dir: "downloaded_arxiv_papers"`) to `config.yaml` and ensure the directory is created if it doesn't exist. Add `arxiv` to `requirements.txt`.

# Test Strategy:
1. Unit test `search_papers` function (mocking `arxiv.Search` and `arxiv.Client().results`): verify correct query construction for various inputs (keywords, date ranges) and parsing of results. 2. Unit test `download_paper` function (mocking `arxiv.Result.download_pdf`): verify correct file path generation and handling of download success/failure. 3. Verify that `pdfs_dir` from `config.yaml` is correctly read and used, and the directory is created.
